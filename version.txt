# Release version of the firmware.
#
# MAJOR and MINOR are bumped deliberately, by hand — they say what release
# this is meant to be. PATCH is counted rather than chosen: a commit hook
# increments it and stages the result, so every commit carries its own. The
# build itself never writes here.
MAJOR=0
MINOR=1
PATCH=2
